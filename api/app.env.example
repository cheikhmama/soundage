# Copy to app.env and set your values. Never commit app.env.
# Used by: api service in docker-compose (api/app.env)

SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/soundage
SPRING_DATASOURCE_USERNAME=your_db_user
SPRING_DATASOURCE_PASSWORD=your_db_password
JWT_SECRET=your-base64-or-hex-secret-at-least-256-bits
